# Public Data Lens — DESIGN.md

> 제품 설계 원칙. 이 문서는 **무엇을 왜** 만드는지를 정한다.
> 구체적 구현·필드 매핑·컴포넌트 계약은 `docs/UI_IMPLEMENTATION_GUIDE.md`에 있다.
>
> 적용 범위: `apps/web` (공공 웹 표면). 컨시어지는 별도 제품 표면이며 이 문서를 따르지 않는다(§9).

---

## Vision

공공데이터 렌즈는 검색 사이트가 아니다. 사람과 AI가 공공데이터를 **쓰기 전에** 발견하고
이해하고 평가하도록 돕는 **데이터 관측소(Data Observatory)** 다.

```
발견(Discover) → 이해(Understand) → 평가(Evaluate) → 사용(Use)
```

웹은 이 탐색 모델을 시연한다. MCP는 같은 능력을 AI 어시스턴트 안으로 옮긴다.
컨시어지는 반복적 계획·검증·실행을 수행한다.

---

# 1. 제품 정체성

## 위치

공공데이터 렌즈는:

- 공공데이터 **탐색 계층**이다
- **근거 지향** 관측소다
- **AI-ready 메타데이터 인프라**다

공공데이터 렌즈가 아닌 것:

- data.go.kr의 대체물
- 챗봇
- 대시보드
- 카드 카탈로그
- 범용 AI SaaS

## 제품 약속

사용자는 다음 네 가지를 알고 떠나야 한다.

1. **무엇이 존재하는가**
2. **무엇이 실제로 관측되었는가**
3. **무엇이 아직 알려지지 않았는가**
4. **다음에 무엇을 확인해야 하는가**

근거가 뒷받침하는 것 이상을 약속하지 않는다.

## 이 제품의 차별점은 근거 그 자체다

경쟁 관계에 있는 것은 포털의 검색창이 아니라 **"이 데이터를 써도 되는지 판단할 수 없는 상태"** 다.
그래서 이 제품이 파는 것은 목록이 아니라 판정의 근거다: 어떤 규칙으로, 어느 스냅샷에서,
어떤 근거 수준으로 판단했는가.

이 판단 근거는 이미 서버가 버전 관리하며 생산하고 있다(판정 규칙 레지스트리 전체 —
`/api/status`의 `service.rules[]`로 조회, 응답 봉투의 `ruleVersions[]`, 근거 수준 2종). **UI의 일이 그것을 만드는 게 아니라 보이게 하는 것이다.**
근거를 숨기거나 요약해서 없애면 제품의 유일한 차별점이 사라진다.

---

# 2. 제품 원칙

1. **검색보다 탐색** — 질의 응답이 아니라 판단 과정을 지원한다
2. **장식보다 근거** — 화면의 모든 주장에는 출처가 붙는다
3. **홍보보다 구조** — 무엇을 할 수 있다고 말하기 전에 무엇이 있는지 보여준다
4. **진행적 공개** — 한 번에 전부 펼치지 않는다
5. **명시적 시스템 상태** — 스냅샷·커버리지·지연을 숨기지 않는다
6. **기능하는 렌즈** — 렌즈는 관점의 전환이며 탭 장식이 아니다
7. **결정론적 사실** — 재현되어야 하는 판정은 서버가 한다
8. **정직한 한계** — 모르는 것을 모른다고 말한다
9. **기본값으로서의 접근성** — 나중에 붙이는 항목이 아니다
10. **점진적 향상** — 기본 기능은 최소 환경에서도 동작한다

모든 UI 결정은 시각적 신기함이 아니라 **신뢰**를 강화해야 한다.

---

# 3. 정보 구조

*(2026-08-04 재배분: 홈은 검색 단독, 블록은 화면별로 이동. 블록 자체와
데이터 출처 원칙은 불변.)*

```
Header                    둘러보기 · 소개 · MCP 연결 (변경 이력은 푸터)
홈(/)
  Hero(축소) + 검색바      검색으로 시작해 검색으로 끝난다
둘러보기(/explore)
  Exploration Story       탐색이 어떻게 진행되는가 — 실제 plan 응답의 과정 서사
  Dataset Anatomy         실파일 관측이 무엇인지 실물로 보여준다
  Live Exploration        구조까지 볼 수 있는 최신 데이터 실물
소개(/about)
  서비스 설명·현황
  Coverage                무엇을 알고 무엇을 모르는지 분모와 함께
  Open Infrastructure     판정 규칙·스키마·평가 지표 공개
MCP 연결(/connect)
  Capability Demo         AI 안에서 같은 능력을 쓰는 방법 → 등록 안내
Footer
```

내비게이션은 기능 마케팅이 아니라 **서사**를 따른다.

핵심 제약: **각 블록은 이미 존재하는 서버 응답으로 채워져야 한다.** 채울 데이터가 없는
블록은 만들지 않는다. 블록별 데이터 출처는 구현 가이드 §3에 명시한다.

---

# 4. 경험 모델 — 세 가지 읽기 깊이

| 층 | 목적 | 성격 |
|---|---|---|
| **L1** | 빠른 이해 | 무엇인지, 볼 가치가 있는지 |
| **L2** | 데이터셋 이해 | 구조·범위·한계 |
| **L3** | 기술적 근거 | 규칙 버전·정규화 원본·정본 JSON-LD |

모든 화면은 세 깊이를 지원한다. L3는 기본 접힘이며, **L3가 존재한다는 사실 자체는 L1에서
보여야 한다** — 근거를 열 수 있다는 것이 신뢰의 근거다.

---

# 5. 렌즈 모델

렌즈는 같은 대상을 다른 질문으로 보는 것이다. 기술 뷰 전환이 아니다.

| 렌즈 | 대답하는 질문 | 상태 |
|---|---|---|
| **Overview** | 이게 무엇이고 볼 가치가 있는가 | 구현 |
| **Structure** | 실제 파일에 어떤 컬럼이 있는가 | 구현 |
| **Evidence** | 왜 이렇게 판정했는가 | 구현 |
| **Possible Uses** | 내 목적에 어떤 역할을 할 수 있는가 | 조건부 — 아래 참조 |
| **Raw** (원본 데이터) | 서버 표현의 원문은 무엇인가 (정규화·원본·JSON-LD) | 구현 |

Raw는 예외적으로 기술 뷰다. 초기 설계는 기술 표현을 접힘(L3)으로 격리했으나,
도달성을 우선해 렌즈로 승격했다(2026-08-04). 판정·해석을 덧붙이지 않고
서버 표현을 그대로 보여 준다는 제약은 유지된다.

**Possible Uses에 대한 판단 갱신**: 계약 v1.4.0의 `build_data_plan`은 **LLM 없이 결정론적으로**
목적 문장에서 후보를 조립하고 역할(PRIMARY/DEMAND/SUPPLY/SPATIAL/TEMPORAL/REFERENCE/RELATED),
근거 신호, 미충족 요구, 다음 확인사항을 산출한다. 서버가 확정하지 않는 것도 계약에 못 박혀
있다 — 품질은 `NOT_ASSESSED`, 결합 가능성은 `CANDIDATE_ONLY`, 계획은 항상 `DRAFT`.

즉 이 렌즈는 프론트엔드 판정 없이 구현 가능하다. 전제는 두 가지다: (1) v1.4.0 배포,
(2) REST 경로 추가(현재 MCP Tool 전용). 사람이 검수한 사례 데이터를 기다릴 필요는 없다.

**만들지 않는 렌즈**

`Readiness` · `Quality` · `Relations`. 서버 근거가 없다. 계약은 `kdp:qualityTier`와
`kdp:diagnosticMaturity`를 `null` 고정으로 명시한다. 프론트엔드에서 이런 점수를 신설하는
것은 §7 원칙 위반이며, 이 제품이 파는 신뢰를 정면으로 훼손한다.

---

# 6. 컴포넌트 언어

| 컴포넌트 | 역할 |
|---|---|
| `DatasetRow` | 결과 1건의 밀도 높은 표현 |
| `EvidenceRow` | 근거 수준·규칙 버전·관측 출처의 일관된 표기 |
| `CoverageIndicator` | 무엇을 알고 무엇을 모르는지, 항상 분모와 함께 |
| `LensNavigation` | 렌즈 전환. L3 기술 표현은 '원본 데이터' 렌즈로 진입한다 |
| `ExplorationTrail` | 어디를 거쳐 여기 왔는지 |
| `WarningPanel` | 서버 경고·오류의 단일 표시 지점 |
| `SourceBadge` | 출처(포털·관측·규칙)의 최소 표기 |
| `Timeline` | 스냅샷·변경 이력의 시간 표현 |

모든 컴포넌트는 장식보다 **밀도와 근거**를 우선한다.

원칙: **컴포넌트는 자기가 어떤 테마에서 렌더되는지 몰라야 한다.** 브랜드 색을 직접
참조하지 않고 의미 토큰만 참조한다(§7).

---

# 7. 시각 언어

## 방향

절제된 구조 · 낮은 시각 소음 · 얇은 경계선 · 중립 표면 · **상태 지향 색**.

발광, 글래스모피즘, 과도한 그라디언트를 쓰지 않는다. 카드는 특별한 콘텐츠에만 쓰고,
목록은 목록으로 표현한다.

이 방향은 취향이 아니라 제품 정의에서 나온다. 관측소는 관측 대상을 압도하지 않는다.

**코어 표면 팔레트 — Data Observatory(2026-08-04).** 유채색 액센트는
deep teal(`#0c6b70`) 하나다. 행정 시스템 연상이 강한 밝은 파랑을 액센트에서 제외했다.
캔버스는 따뜻한 백색(`#faf9f7`), 텍스트는 먹색 네이비 잉크(`#151c28`·`#141b26`),
구조선은 차가운 회청색 — 따뜻한 캔버스 위 차가운 구조선의 온도차가 관측 기구의
인상을 만든다. 상태 태그의 모서리는 필(999px)이 아니라 6px — 전부 둥글리면 범용
AI SaaS 문법으로 회귀한다. 근거(녹색)·한계(황갈)·미지(회색) 의미색은 불변.

## 색의 의미 축은 계약에서 도출된다

이 제품의 색 체계에서 가장 중요한 결정은 팔레트가 아니라 **의미 축**이다. 그리고 의미
축은 디자이너가 발명하지 않는다 — 이미 계약 enum으로 정의되어 있다: 근거 수준 2종,
지역 근거 5종, 수집 상태 10종, 최신성 3종, 예시값 상태 6종, 변경 상태 6종.

**절대 규칙 — 미수집·불명·보류에 실패색을 쓰지 않는다.**

```
NOT_COLLECTED / QUEUED / COLLECTING       →  중립. 수집 순번이며 품질 문제가 아니다
MISSING_FROM_SNAPSHOT                     →  중립. 폐기 확정이 아니다
UNKNOWN (freshness)                        →  중립. 나쁨이 아니다
INFERRED_* (region)                        →  중립. 틀렸다는 뜻이 아니다
WITHHELD_BY_LICENSE / WITHHELD_BY_SAFETY   →  중립. 정책적 보류다
ACCESS_RESTRICTED / SOURCE_UNAVAILABLE     →  중립
```

경고색·오류색은 실제 오류(`error.code` 9종)와 서버 경고(`warnings[]`), 그리고
`POSSIBLY_STALE`·`OFFICIALLY_WITHDRAWN`·`COLLECTION_FAILED`에만 쓴다.

이유: 사용자는 문안보다 색을 먼저 읽는다. 서버 계약이 "미수집 ≠ 품질 문제"를 명문화했는데
UI가 그것을 빨갛게 칠하면 **UI가 계약을 배신한다.** 이 규칙은 시각 취향이 아니라
정확성 요건이다.

## 토큰 층

세 층으로 분리하며, 컴포넌트는 3층에서 2층만 참조한다.

```
1층  팔레트    테마별로 완전히 다름. 웹과 컨시어지가 여기서 갈린다
2층  의미      계약 enum에서 도출. 테마 무관. 이름이 양쪽에서 동일
3층  컴포넌트  2층만 참조. 1층을 직접 참조하지 않는다
```

브랜드 토큰이 의미 역할을 겸하면 테마 교체가 선택자 단위 오버라이드를 요구하게 된다.
분리의 목적은 미학이 아니라 **두 표면의 독립 진화**다.

## 타이포그래피

| 역할 | 서체 |
|---|---|
| 본문·UI (한글) | Pretendard |
| 본문·UI (라틴·숫자) | Inter |
| 데이터·코드·식별자 | IBM Plex Mono |

**조달 결정(2026-08-04): npm 셀프호스팅.** Pretendard 가변 **동적 서브셋**
(`pretendard` 패키지 — 한글 2,574자 분할 로드, 라틴은 Inter 파생이라 Inter 별도 조달
불필요)과 `@fontsource/ibm-plex-mono`(400·600)를 번들에 포함한다. CDN을 쓰지 않는
이유: 외부 요청 없는 자기완결 배포(개인정보 고지와 정합)와 오프라인 재현성.

**모노스페이스의 용도를 좁게 유지한다.** `recordId`, `listKey`, `ruleId`, 원본 컬럼명,
enum 코드 — 즉 **기계가 정한 문자열**에만 쓴다. 사람이 쓴 문장에는 쓰지 않는다. 이
경계가 지켜지면 서체 자체가 "이건 계약값이다"라는 신호가 된다.

## 시그니처

이 제품이 기억될 단 하나의 요소는 **근거 표기(evidence marker)** 다.

화면의 모든 판정 옆에 근거가 일관된 형태로 붙고, 그 표기가 어디서나 같게 읽힌다.
사용자가 서비스를 떠난 뒤 기억하는 것이 "근거가 항상 붙어 있었다"여야 한다.

보편적 규칙으로: **판정이 있으면 근거가 있고, 근거가 없으면 판정하지 않는다.**

과감함은 이 한 곳에만 쓴다. 나머지는 조용하고 규율 있게 유지한다.

---

# 8. 언어 규칙

## 권장

```
후보 (candidate)              관측됨 (observed)          확인됨 (confirmed)
메타데이터 (metadata)          파일 관측 (file observation)
가능한 활용 (possible use)     추가 확인 필요 (additional verification required)
아직 관측되지 않음             기재됨 / 미기재
```

## 회피

```
추천 (recommended)    보장 (guaranteed)    AI-ready    적합 (suitable)
완전 (complete)       최고 (best)          최적 (optimal)
```

서버 근거가 명시적으로 뒷받침하지 않으면 쓰지 않는다.

## 문안 작성

- **화면에 있는 말은 이해를 돕기 위해 존재한다.** 장식이 아니라 설계 재료다.
- 시스템 구조가 아니라 **사용자가 인식하는 대상**의 이름을 쓴다.
- 능동태를 기본으로. 컨트롤은 눌렀을 때 벌어지는 일을 그대로 말한다.
- 하나의 요소는 하나의 일만 한다. 라벨은 라벨이고, 예시는 예시다.
- **오류와 빈 상태는 분위기가 아니라 방향을 준다.** 무엇이 일어났고 무엇을 하면 되는지
  말한다. 사과하지 않고, 모호하지 않다.
- **빈 결과는 데이터의 부재가 아니다.** "찾지 못했습니다"로 끝내지 않고 조회 범위
  (스냅샷·모집단)를 함께 말한다.

## 계약 문안은 프론트엔드가 다시 쓰지 않는다

서버 `warnings[]`의 문안을 화면에서 임의로 치환하지 않는다. 사용자 언어로 다듬을 필요가
있으면 서버 계약에 구조화된 필드를 추가하는 방식으로 해결한다. 원문을 툴팁에만 남기는
것은 스크린리더·모바일에서 근거가 사라지는 것과 같다.

---

# 9. 웹 · MCP · 컨시어지

| 표면 | 역할 | 시각 시스템 |
|---|---|---|
| **웹** | 대표적 탐색 시연 | 관측소 — 이 문서가 규정 |
| **MCP** | 반복 가능한 AI 워크플로 | 해당 없음 |
| **컨시어지** | 계획 · 검증 · 실행 | **별도 테마. 이 문서를 따르지 않는다** |

세 층은 상호보완이며 무료/유료 등급이 아니다.

## 웹과 컨시어지의 시각 분리

두 표면은 영구적으로 다른 시각 시스템을 쓴다. 그러나 **컴포넌트는 공유한다** —
컨시어지가 데이터셋 프로필과 검색을 호출하므로 같은 컴포넌트가 양쪽에서 렌더된다.

따라서 갈라내는 것은 앱도 파일도 아니라 **팔레트 층 하나**다(§7 토큰 층). 컴포넌트가
의미 토큰만 참조하면 팔레트 블록 교체로 테마가 바뀐다.

컨시어지 전용 스타일은 물리적으로 분리하고 해당 표면에서만 로드한다. 공개 빌드에
컨시어지 스타일이 실려 나가지 않는다.

---

# 10. 안티패턴

하지 않는다:

- 카드 남용 — 목록은 목록으로 표현한다
- **readiness·quality·suitability 점수 신설** — 서버 근거 없는 판정
- **판별력 없는 종합 점수를 진행 막대로 표현** — 대다수가 같은 값이면 막대는 정보가 아니라
  장식이다. 무엇이 기재됐는지가 실제 정보다
- 불확실성 은닉 — 스냅샷 지연, 커버리지 분모, 추론 여부를 숨기지 않는다
- 장식적 AI 비주얼
- **백엔드 로직의 프론트엔드 복제** — 결정론적 판정을 화면에서 다시 만들지 않는다
- **원시 JSON 덤프를 L3라고 부르는 것** — 계층 없음은 진행적 공개가 아니다
- 배포되지 않은 기능 광고
- 툴팁에만 존재하는 정보 — 모바일·보조기술에서 접근 불가

---

# 11. 성공 지표

성공한 UI는 사용자가 다음 다섯 질문에 답할 수 있게 한다.

1. **어떤 데이터가 존재하는가?**
2. **왜 이것이 보여졌는가?**
3. **어떤 근거가 이를 뒷받침하는가?**
4. **무엇이 아직 알려지지 않았는가?**
5. **다음에 무엇을 확인해야 하는가?**

다섯 질문 모두 **화면에서** 답해져야 한다. 툴팁·개발자 도구·GitHub 저장소가 아니다.

## 품질 하한선

발표하지 않되 반드시 지킨다.

- 모바일까지 반응형
- 키보드로 도달 가능하고 포커스가 보이는 모든 인터랙티브 요소
- `prefers-reduced-motion` 준수
- 색만으로 상태를 전달하지 않음 — 항상 텍스트 병기
- 계약 enum에 라벨 누락 없음
